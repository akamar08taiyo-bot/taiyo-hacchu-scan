# CLAUDE.md — taiyo-hacchu-scan（販売受注簿作成ツール）

## セッション開始時の手順
1. まず `Learnings.md` を読み、過去に発生した失敗パターン・ユーザーの好み・
   繰り返し発生した確認事項を把握してから着手する。
2. 同じ失敗（下記「既知の落とし穴」参照）を再発させない。
3. 不明点は推測で進めず、実データ（ソース・CSS・過去コミットメッセージ）を
   確認してから回答・実装する。

## セッション終了・区切り時の手順
まとまった作業が終わったら、以下の観点で `Learnings.md` に追記する。
- ユーザーが修正を求めた箇所（何を・なぜ）
- 同じ確認・同じ手戻りを繰り返した箇所とその回避策
- 今後のセッションで再利用できる業務ルール・コードパターン
知見は「起きた事実→原因→今後の対処」の形で、具体的なファイル名・関数名・
コミット相当の内容を添えて書く。抽象的な精神論は書かない。

## プロジェクト概要
- 用途: 介援隊などのカタログ画像をスクリーンショット→OCR(tesseract.js)で
  読み取り、**販売受注簿**（受注区分: 販売／給付／在庫）を自動作成するツール。
  受注簿から**見積書**（御見積書）をワンクリックで別ウィンドウ生成できる。
- 公開ポータル `taiyo-portal`（別リポジトリ）から「販売受注簿作成ツール」として
  リンクされている。太陽シルバーサービス社内の業務アプリ群の一つ。
- 技術構成: Vite + React（`src/main.jsx`, 1ファイルに集約・圧縮された生成コード
  相当のスタイルでベタ書きされている＝手で読むには `grep -n -o` でパターン検索が
  現実的。整形されていないので awk/sed で該当行を fold して読むこと）。
  スタイルは `src/styles.css`。OCRは `tesseract.js`、PDF化は `jspdf`+`html2canvas`。
- 受注簿本体はReactコンポーネント。見積書・PDF帳票は**文字列連結でHTMLを
  組み立てて `window.open` / Blobで別ウィンドウ表示**する方式（JSXではない）。
  修正対象がどちらの方式か、まず特定してから編集すること。

## ビルド・公開の運用ルール
- `npm run build`（vite）→ `dist/` にハッシュ付きファイル名で出力される。
- **GitHub Pagesの公開元は `docs/`**。`cp -r dist/. docs/` で反映する。
- ハッシュ付きファイル名が変わるため、`docs/assets/` に**古いハッシュのファイルが
  残り続ける**。`docs/index.html` と `assets/index-*.js` から参照されている
  ファイル名だけを残し、参照されなくなったものは必ず削除してからコミットする。
  例:
  ```sh
  cd docs && for f in assets/*; do n=$(basename "$f"); \
    grep -q "$n" index.html assets/index-*.js 2>/dev/null || rm -f "$f"; done
  ```
- `npm install`/`npm run build` は無関係に `package-lock.json` を書き換えることが
  ある。依存関係を意図的に更新したのでなければ、コミット前に
  `git checkout -- package-lock.json` で差分を消す。
- デフォルトブランチは **`master`**（`main` ではない）。他の姉妹リポジトリと
  混同しないこと。

## テスト
- `node --test "test/*.test.mjs"` — 通常のユニットテスト（レイアウト・給付計算・
  見積書の太字禁止/構造など）。
- `node test/run-scan-tests.js` — `node --test` には含まれない、介援隊カタログの
  OCR解析を実データで検証する独立スクリプト。**両方**実行して確認すること。
- UIの見た目・PDFの崩れなどを直したときは、Playwright を使い
  実際にボタンを押させて検証する（下記「実機検証パターン」参照）。
  スクリーンショット目視だけで「直った」と判断しない。

## 実機検証パターン（見積書PDFなど）
- `vite preview` または `http-server` でビルド済み成果物を配信し、Playwrightで
  実際のUI操作（値の入力・ボタンクリック）を行ってから検証する。
- PDF自体の中身を検証したいときは、画面のスクリーンショットではなく
  **実際に「PDF保存」ボタンを押させて生成されたPDFのバイト列を取得**して検証する。
  この帳票は `window.opener.__taiyoPdfLibs`（jsPDF/html2canvas）をポップアップ
  ウィンドウから使う設計なので、`URL.createObjectURL` を親ウィンドウ側でフックして
  生成された `Blob` を横取りするのが確実（画面キャプチャより正確）。
- ウィンドウ幅を変えて（狭い幅・広い幅）同じ操作を行い、**同一のPDFが出るか**を
  確認する（下記「既知の落とし穴」参照）。

## 既知の落とし穴（再発防止）
1. **給付モードのCSSが複数世代重なって破綻した事例**: `.benefitMode` 関連の
   CSSが2026-07-09/07-10/07-11/08-30と複数回追記され、後発の
   `:has(...)` セレクタが `.management` を `display:contents` にして
   grid-template-areas を別名で組み替えていたため、後から入った指定と
   噛み合わず管理情報が消える・要素が重なる不具合になっていた。
   → **同じ機能に対する過去のCSS上書きを見つけたら、古い世代を削除してから
   新しい指定を1つだけ残す**。積み増しで直そうとしない。
2. **PDFがウィンドウ幅に引きずられる**: `html2canvas` はそのときの画面幅の
   まま要素を取り込むため、ウィンドウが900px以下（スマホ用CSSが効く幅）だと
   PDFもスマホレイアウトのまま出力されていた。
   → PDF取り込み直前だけ `<html>` に固定クラス（例: `pdfExport`）を付け、
   幅に関係なくA4想定のデスクトップレイアウトを強制し、取り込み後に外す。
3. **わずかな高さ超過で2ページに割れて途中の行が消える**: 1px単位の超過でも
   ページ分割される実装だと、行の途中で無意味に切れる。
   → 「A4の一定倍率（例1.45倍）を超えるまでは縮小して1ページに収める」の
   ように許容範囲を持たせる。
4. **太字の直しは1回で全部やる**: 「太字が目立つ」という指摘に対し、要素を
   小出しに直すと「ここも」「あそこも」と何度も指摘を受けることになった
   （表題・社名・合計金額をそれぞれ別ターンで指摘された）。
   → 太さを直すときは、対象CSSブロック全体を `font-weight` で機械的に
   grepして一覧化し、**残す強調（通常1〜2箇所）以外は全部落とす**。
5. **「1行あける」「列をずらす」は境界を早合点しない**: 最初「商品名と送料の間」に
   空行を入れたが、ユーザーの意図は「品目群（商品＋送料）」と「小計以下」の
   境界だった。表の余白指示は、どの2グループを区切りたいかを
   具体的な行名で確認（or 両方試して見せる）してから直す方が手戻りが少ない。
6. **業務ツール名とリポジトリ名は一致しない**: 「販売受注簿」はこのリポジトリ
   (`taiyo-hacchu-scan`) だが、消耗品売上管理は別リポジトリ
   (`taiyo-uriage-bunseki`)。どのツールがどのリポジトリかは
   `taiyo-portal`（社内ポータル）の `index.html` 内 `APPS` 配列の
   `name`/`desc`/`url` が正。ツール名だけで推測せず、まずポータルで確認する。

## デザイン方針（このリポジトリの帳票全般）
- 受注簿・見積書とも「太字は使いすぎない」。強調は文字サイズ・色・背景色で
  付け、`font-weight` は本文500・見出しも500を基本にする（700以上は
  ほぼ使わない）。表題であっても例外にしない。
- 給付にチェックを入れても、販売（チェックなし）のときの項目・レイアウトは
  そのまま残し、給付関連の項目は「追加」する形にする（既存を消したり
  作り変えたりしない）。
- 施設名など長いテキストの折り返しは、ブラウザ標準の `<datalist>` に頼らず
  自作の候補リスト／自動リサイズ／Shift+Enter・Alt+Enterでの手動改行など、
  挙動を完全に制御できる実装にする（環境依存の見た目崩れを避けるため）。
