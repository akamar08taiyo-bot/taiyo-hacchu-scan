# Learnings.md — セッション振り返りログ

このファイルはセッションをまたいだ知見の蓄積用。新しい振り返りは**末尾に追記**する
（過去の記録は書き換えない）。各エントリは「起きた事実 → 原因 → 今後の対処」の形式。
`CLAUDE.md` の「既知の落とし穴」に一般化できるものは、あわせてそちらにも反映する。

---

## 2026-09-04〜06 セッション（給付UI崩れ／見積書PDF崩れ・体裁調整）

### 1. 給付チェックでUIが崩れる（起点となった不具合）
- **事実**: 受注区分「給付」にチェックを入れると、管理情報（定価・仕切り・
  販売単価など）が消えたり、給付内訳と重なって表示されたりした。
- **原因**: `.benefitMode` 向けのCSS上書きが2026-07-09/07-10/07-11/08-30と
  4世代分積み重なっていた。特に07-11の `:has(.orderTypeChecks label:nth-child(2)
  input:checked)` セレクタは特異度が高く、`.management` を `display:contents` にし
  `grid-template-areas` を独自の名前（managementTitle/managementBody/benefit）で
  組み替えていたため、08-30に追加された `benefitManagement/benefitImage` 前提の
  指定と噛み合わなくなっていた。
- **対処**: 該当機能の古いCSS世代をすべて削除し、最新の意図（管理情報の表＋
  給付内訳を縦に並べるだけ）を1つのルールセットに書き直した。
- **今後**: 同じクラス名・同じ機能に対する複数世代のCSSがないか、修正前に
  `grep -n "\.benefitMode\|:has(" src/styles.css` 等で必ず洗い出す。
  「動かないから上に足す」を繰り返すと将来また同じ壊れ方をする。

### 2. 見積書PDFがウィンドウ幅で壊れる
- **事実**: PC幅を狭くして「見積もり作成」→「PDF保存」すると、スマホ用の
  1カラムレイアウトのままPDF化され、かつ内容がわずかにA4を超えると2ページに
  分断されて行の途中で切れていた。
- **原因**: `html2canvas` はキャプチャ時点の実際のDOMをそのまま描画するため、
  画面のCSSメディアクエリ（`@media (max-width:900px)`）が発火した状態で
  キャプチャされていた。ページ分割も「A4の高さを1pxでも超えたら分割」という
  実装で、余白調整をしていなかった。
- **対処**:
  - `html.pdfExport` クラスを取り込み直前だけ付与し、`.sheet`/`.quoteTop`/
    `.companyBox` などをメディアクエリに関係なくA4デスクトップ幅に固定。
    取り込み後は `finally` で必ず解除する。
  - ページ分割の閾値を「A4の1.45倍を超えるまでは縮小して1ページに収める」に変更。
- **今後**: 印刷・PDF化を伴う画面は、**必ず複数のウィンドウ幅で同じ操作をして
  出力が一致するか**を確認する（狭い幅だけ壊れるパターンが多い）。

### 3. 太字の直しを3回に分けて指摘された
- **事実**: 「太字が目立ちすぎる」→（一部修正）→「社名・合計金額の太字も不要」
  →（修正）→「表題の文字も太字にしない」と、都合3ターンに分けて同じ種類の
  指摘を受けた。
- **原因**: 最初の修正で「太字をやめる」対象を機械的に洗い出さず、目についた
  要素（宛名・見出し・品名など）だけを個別に直したため、社名・合計金額・表題が
  取りこぼされていた。
- **対処**: 最終的に `font-weight:\s*(?:[7-9]00|bold)` を帳票CSS全体から
  正規表現で検索し、**1件も残っていないこと**をテストで担保する形にした
  （`test/estimate-layout.test.mjs` 参照）。
- **今後**: 「◯◯をやめて」系の指摘（太字・色・余白など見た目の一括修正）は、
  最初から対象CSSブロック全体を該当プロパティでgrepして一覧化し、
  意図的に残す例外（今回は0件）以外は全部直す。個別対応で小出しにしない。

### 4. 表の余白・整列の指示を早合点した
- **事実**: 「商品名から1行あけて小計・諸経費・消費税」という指示を、最初は
  「商品行と送料行の間」に空行を入れる形で実装したが、実際は
  「品目群（商品＋送料）全体」と「小計以下」の境界だった。その後の指摘で
  「商品名と送料は1行あける必要はない」と訂正が入った。
- **今後**: 表の余白・区切りの指示は、区切りたい2つのグループを具体的な
  行名（例:「商品行・送料行」対「小計・消費税・合計」）で自分の理解として
  言語化してから実装する。曖昧なまま実装して手戻りするより、実装前に
  一度整理した方が速い。

### 5. 業務ツール名とリポジトリの対応が非自明
- **事実**: ユーザーが「販売受注簿の作成」と言った対象は、セッション開始時に
  接続されていた `taiyo-uriage-bunseki`（消耗品売上管理）ではなく、
  別リポジトリ `taiyo-hacchu-scan`（カタログOCR→受注簿作成ツール）だった。
- **対処**: 社内ポータル `taiyo-portal/index.html` の `APPS` 配列
  （name・desc・url）を確認し、ツール名からリポジトリを特定した。
- **今後**: 「◯◯の修正をしたい」で対象が曖昧なときは、まずポータルの
  `APPS` 一覧で名称照合する。早い段階でこれをやれば、無関係なリポジトリを
  探索する手戻りを避けられる。

### 6. ビルド公開の運用で見落としやすい点
- **事実**: `npm run build` の出力（`dist/`）を `docs/` にコピーしても、
  ハッシュ付きファイル名が変わるため**古いバンドルが `docs/assets/` に残り続ける**。
  また `npm install`/`npm run build` が `package-lock.json` を無関係に
  書き換えることがあった。
- **対処**: `docs/index.html` とエントリJSから参照されているファイル名だけを
  残し、残りは `rm` してからコミット。`package-lock.json` は意図した変更で
  なければ `git checkout --` で戻す。
- **今後**: 公開ビルドの反映は「build → docs/へコピー → 参照されない
  assetsを削除 → package-lock.jsonの意図しない差分を確認」を毎回セットで行う。

### 7. 有効だった検証手法（継続する）
- Playwright で実際に「PDF保存」ボタンをクリックさせ、`URL.createObjectURL`
  を親ウィンドウでフックして生成された実PDFのバイト列を取得・検証する方法は、
  画面のスクリーンショットだけで判断するより確実だった（見た目は正しくても
  実際のPDF出力だけが古いままということがあり得るため）。継続して使う。
- 修正前後で同じ操作をして生成物（PDF/画像）を比較し、「直したい箇所以外は
  ピクセル単位で変わっていないこと」（例: 給付なしモードのスクリーンショットの
  md5一致）を確認したのも有効だった。

---

## 2026-09-09 見積書PDFで発行日が切れる

### 8. 帳票内に `<input>` を置くとPDF（html2canvas）で文字が切れる
- **事実**: 見積書をPDF保存すると「発行日」の日付だけ下半分が切れて潰れていた。
  画面表示・ブラウザ印刷では正常で、PDF保存のときだけ発生していた。
- **原因**: 見積書の編集項目はすべて `contenteditable` の `<span class="editable">`
  で統一されているのに、**発行日だけ `<input id="quoteDate">` になっていた**。
  html2canvas は input の値テキストを独自ロジックで描画し、ブラウザ本来の
  垂直中央揃えと合わないため、テキストが下にずれて枠外が切り落とされていた。
- **対処**: 発行日を他の項目と同じ `<span class="editable companyMetaValue"
  contenteditable="true" data-field="quoteDate">` に置き換え、不要になった
  `.companyMeta input` セレクタも削除した。
  `test/estimate-layout.test.mjs` に「帳票ビルダー内に input/textarea を
  生成しないこと」の検査を追加（ツールバーの select は印刷に出ないので対象外）。
- **今後**: **html2canvas でPDF化する範囲にフォーム部品（input/textarea/select）を
  置かない**。編集させたい項目は `contenteditable` の span/div にする。
  PDFの不具合調査では「その要素だけ他と実装が違わないか」をまず疑う。

### 9. 「手打ちできない」と感じさせる入力欄（datalist）
- **事実**: 営業担当を手打ちで変えたいという要望。実装上は `<input>` なので
  打ち込めてはいたが、`<datalist>` の候補ポップアップが出るため
  「リストからしか選べない欄」に見えていた。
- **原因**: `Nt` コンポーネントが `<input list>` + `<datalist>` 方式だった。
  さらに datalist の id を毎レンダリングで `Math.random()` から作っていたため、
  1文字打つたびに datalist が作り直され、ポップアップが不安定だった。
  姉妹アプリ（taiyo-uriage-bunseki）でも同じ理由で datalist をやめている。
- **対処**: `Nt` を「素の input ＋ 自前の候補リスト」に置き換えた。
  フォーカス時は登録名を全件、打ち始めたら部分一致で絞り込み、現在値は候補から除外。
  候補クリック・Escape・外側クリック・スクロールで閉じる。
  この部品は営業担当／発注者／発注先／型番の4箇所で共有しているので、まとめて改善された。
- **落とし穴**: 候補リストを `position:absolute` で出すと、親の
  `.entryGridPrimary` などが**角丸のため `overflow:hidden`** になっており切り取られる。
  入力欄の実座標を `getBoundingClientRect()` で測って `position:fixed` で置くことで解決した。
- **今後**: 候補付きテキスト入力は `<datalist>` を使わず自前で描く。
  ドロップダウンを作るときは、祖先に `overflow:hidden` がないか
  （角丸のために付いていることが多い）を先に確認する。
- **テスト実装の注意**: `src/main.jsx` にはReact本体がバンドルされているため、
  `assert.doesNotMatch(source, /datalist/)` のような広い検索は React 内部の文字列や
  自分が書いたコメントに当たって誤検知する。**生成箇所（`jsx)(\`datalist\``）を
  名指しで検査する**こと。

---

## 2026-09-09 販売受注簿のPDF保存

### 10. 印刷用CSSを流用してPDF化する
- **事実**: 見積書と同じように、販売受注簿もボタン一つでPDFにしたいという要望。
  `pdfDownload()` は以前から実装されていたが、どのボタンにも接続されていなかった。
- **問題**: そのまま繋ぐと「画面の見た目」（入力欄・日付ピッカー・セレクトの矢印が
  写った状態）でPDF化されてしまう。受注簿は印刷時に `.printValue` へ差し替える
  作りなので、画面のままでは帳票として使えない。
- **対処**: 取り込みの直前だけ、**`@media print` の中身を実行時に取り出して
  `<style>` として実DOMに流し込む**方式にした（`collectPrintCssText`）。
  印刷用CSSを二重に持たずに済み、印刷とPDFの見た目が自動的に一致する。
  取り込み後は `finally` で必ず外す。
- **あわせて必要だった3点**:
  1. **画面幅で仕上がりが変わる** → 取り込み中は `.orderEntrySheet` を
     A4横の実寸（余白4mmを引いた 289mm×202mm）に固定。1440px幅と820px幅で
     生成PDFのバイト列が一致することを確認した。
  2. **複数ページの二重取り込み** → 複数商品のときは印刷用の控え
     （`.multiPrintPages`）が表示され編集中のシートが隠れる作りなので、
     印刷CSSを当てた**後**に `getBoundingClientRect()` で見えているシートだけを拾う。
     これで印刷と同じ「商品ページ×N＋集計ページ」の並びになる。
     （フィルタしないと隠しシートまで描画してPDFが重複し、90秒でも終わらなかった）
  3. **残る入力欄** → 印刷CSSでも管理情報などの `<input>` は残る。第8項の
     切れる問題を避けるため、html2canvas の `onclone` で複製側の input/select/
     textarea を同じ大きさの `<span>` に置き換えてから描画する
     （ラジオ・チェックはそのままでも正しく描かれるので触らない）。
- **今後**: 画面と帳票で見た目が違うアプリをPDF化するときは、印刷用CSSを
  実行時に流用するのが最も安全。`@media print` を手で複製しない。

### 11. 見積書の明細で色が二重に出る
- **事実**: 見積書の品目欄が「安寿 折りたたみシャワーベンチ FSフィット/536-057 ブルー」
  で、その下の補足行にも「ブルー / 定価35,000円」と出て、色が二度並んでいた。
- **原因**: 商品名（OCRで取り込むと色まで含まれることが多い）と、色・サイズ欄を
  無条件に両方出していた。型番については `!rawProduct.includes(model)` の
  重複チェックがあったのに、色・サイズには無かった。
- **対処**: 色・サイズも商品名に含まれていれば下段に出さない。
  含まれていなければ従来どおり表示する（情報は落とさない）。
- **今後**: OCR由来の項目（商品名・型番・色・サイズ）は**重なって入っている前提**で扱う。
  帳票に並べるときは、既に他の欄に出ている文字列かどうかを確認してから足す。

---

## 2026-09-09 入力途中データの保護

### 12. 閉じると入力が全部消える → 自動保存＋復元の選択
- **事実**: 入力の途中でタブを閉じた・回線が切れたときに、受注簿の入力が
  すべて消えてしまうという指摘。
- **対処**: 受注簿の入力を localStorage（`hatchu-ai-scan-draft-v1`）へ
  600ms のデバウンスで自動保存し、次に開いたときに
  「続きから入力する」／「新しく始める」を選ぶダイアログを出すようにした。
- **実装で注意した点**:
  1. **確認前に上書きしない**: 下書きの読み込みを `useState(()=>readOrderDraft())`
     の初期化で同期的に行い、ダイアログ表示中は自動保存を止める。
     effect で読むと、初回レンダー直後の空の状態で保存処理が走り、
     読む前に下書きを消してしまう。
  2. **容量超過でも文字は守る**: 貼り付け画像や商品ページの画像スナップショット
     （`_imgSnap`）は大きいので、`setItem` が失敗したら「画像なし」→
     「ページ画像も削除」と段階的に保存し直す。文字データの保護を最優先にする。
  3. **空の下書きは残さない**: 顧客名・商品名など主要項目が全部空なら保存せず削除。
     何も入力していないのに毎回ダイアログが出るのを防ぐ。
- **テストの注意**: 複数商品の追加（`addProduct`）は
  「商品情報あり・商品名が画面文言でない・**仕切り(税抜)入力済み**」の3条件を
  満たさないと実行されない。E2Eで複数ページを作るときは仕切りまで入れること
  （これを忘れて「multiSavedが保存されない」と誤診しかけた）。

### 13. 復元ダイアログの破壊的な選択肢には確認を挟む
- **事実**: 下書き復元ダイアログの「新しく始める」は、押した瞬間に前回の入力を
  完全に削除する作りだった。データ消失を防ぐための機能なのに、
  押し間違いで消えてしまうのでは本末転倒。
- **対処**: 削除側だけ `window.confirm` で一度確認する
  （「前回の入力（顧客名）を削除して新しく始めます。よろしいですか？」）。
  復元側（続きから入力する）には確認を挟まない——安全な操作に手間を足さない。
- **今後**: 「データを守るための機能」を作ったら、その機能自身に
  取り返しのつかない操作が混ざっていないか点検する。
  破壊的な分岐にだけ確認を置き、安全な分岐は1クリックのままにする。

---

## 2026-09-11 商品名の読み取り不具合2件

### 14. 商品名が2回並ぶ（同じ見出しを2通りに読んでしまう）
- **事実**: 受注簿の商品名が
  「ラップポン介護用カタメルサーT3/COCOT3P1）6L **ラップポン人**介護用カタメルサーT3/COCOT3P1）6L」
  と、ほぼ同じ文字列が2つ並んでいた。
- **原因**: OCRは psm 6 と psm 11 の2通りで読み取って結果をつないでいる。
  行単位では重複を除いているが、**同じ見出しの別読みが1行に並ぶ**と別物として残る
  （「介護用」と「人介護用」のように1文字違う）。商品名の抽出はその行をそのまま採る。
- **対処**: 商品名の仕上げ（`sanitizeScanName`）で、前半と後半がほぼ同じなら前半だけを使う。
  判定は2文字組（バイグラム）のDice係数で、空白区切りは0.72以上、
  空白なしで続く場合は中央付近で切って0.8以上を重複とみなす。
- **今後**: OCR結果は「同じものを違う読み方で2回返す」前提で扱う。
  行の重複除去だけでは足りない。

### 15. 商品名の先頭の数字が消える
- **事実**: 「4点ステッキS」が「点ステッキS」になっていた（このアプリ自身の
  商品辞書に `4点ステッキS` と登録されているので、正しい名前は数字付き）。
- **原因**: 箇条書き番号を落とすための `replace(/^[\d①-⑳]+[\s.、．]*/,'')` が、
  **区切り記号なしでも**先頭の数字を削っていた。
- **対処**: 丸数字（①など）は従来どおり落とし、通常の数字は
  「1.」「2、」のように区切りが続くときだけ落とす。
- **今後**: 「ノイズ除去」の正規表現は、正当なデータを巻き込まないか
  実際の商品名で確かめてから入れる。特に先頭・末尾を削る処理は要注意。

### 検証について
`test/scan-cases.js` に実例2件を追加し、**修正前のコードでは落ちること**を
確認してから修正を入れた（10/12 → 12/12）。期待値に `productNameIncludes` を
使えるよう `test/run-scan-tests.js` を拡張した。

---

## 2026-09-14 アプリ全体のデバッグ（実機操作で洗い出し）

### 16. CSSの世代重なりで、ヘッダーのツール名が「どこにも表示されない」状態だった
- **事実**: 画面上部に出るのはロゴマークとボタンだけで、ツール名（販売受注簿作成）も
  説明文も表示されていなかった。DOMには文字があるのに幅・高さが 0 だった。
- **原因**: `.brand h1 { font-size: 0 !important }` ＋ `::after { content: "販売受注簿作成" }`
  で差し替える世代のCSSに対し、後発の
  `.app .brand h1::after { content: none !important }` が **::after だけを打ち消して**いた。
  `font-size: 0` は残るので、元のテキストも差し替えテキストも出ない。
  代わりに `index.html` の `window.addEventListener("load", ...)` で文字を入れる作りに
  なっていたが、React の描画と競合して当たらないことがある（実測では当たっていなかった）。
- **対処**: 見出しの文字は **JSX 側に持たせ**（`販` / `販売受注簿作成` /
  `画像を読み取り、販売受注簿へ反映`）、CSSからは `font-size:0` と `::after` 差し替えを
  すべて削除。`index.html` のブートストラップ用スクリプトも削除した。
  `test/debug-fixes.test.mjs` で「font-size:0 が残っていないこと」「::after が無いこと」を検査。
- **今後**: CLAUDE.md の落とし穴1（CSS世代の積み増し）は `.benefitMode` 以外でも起きる。
  **表示文言をCSSの `content` で差し替えない**。文言はマークアップに置き、CSSは体裁だけにする。
  「DOMに文字があるのに見えない」ときは `getComputedStyle` の font-size / width / height を疑う。

### 17. 給付の3割負担で1円が消えていた（浮動小数点）
- **事実**: 商品代金 11,000円・3割負担のとき、利用者負担 3,300円／保険者負担 **7,699円**。
  足すと 10,999円で 1円合わない。印刷した受注簿にもそのまま出ていた。
- **原因**: `保険者負担 = Math.floor(c * (1 - 0.3))`。`1-0.3` の誤差で
  `floor(11000*0.7) = 7699` になる。1割・2割では誤差が出ないため気付きにくい
  （500,000円までの全数検査で3割だけ 9,357件ずれていた）。
- **対処**: `利用者負担 = Math.ceil(c * 割 / 10)`（整数どうしの掛け算）、
  `保険者負担 = 対象額 − 利用者負担` に変更。差額で出すので合計は必ず一致する。
  1割・2割の結果は 300,000円まで全数で **変化なし**であることを確認してから入れた。
- **今後**: 金額の按分は `x * 0.7` のような小数係数を使わず、
  **`x * 整数 / 10` と「残りは差額」**で出す。片方を丸めたらもう片方は引き算にする。

### 18. 逆算入力（利益額・利益率）が送料ぶんずれていた
- **事実**: 利益額に 30,000 と入れると 31,200 に、利益率に 25 と入れると 26.1 になった。
- **原因**: 画面の利益額は `送料込合計(税抜) − 仕切り合計` で出しているのに、
  逆算側（`nt` / `rt`）は送料を無視して `販売金額 − 仕切り` で解いていた。
- **対処**: `nt` / `rt` に送料を渡し、`販売金額 = 仕切り合計 + 利益額 − 送料`、
  `販売金額 = 仕切り合計/(1−利益率) − 送料` で解くようにした。
- **今後**: 「表示している式」と「入力から逆算する式」は必ず対にして直す。
  片方だけ直すと、打った数字と出る数字が違うという一番気付きにくい不具合になる。

### 19. 複数商品の見積書に送料の行が無く、明細と小計が合わなかった
- **事実**: 2商品＋送料1,200円のとき、明細は 84,000円＋20,480円＝104,480円しか無いのに
  小計（税抜）は 105,680円。1行目の消費税も 8,520円（＝商品分8,400＋送料分120）で、
  金額×10% になっていなかった。お客様に出す書類として説明できない。
- **原因**: 複数商品版は `estimateItemRow` で商品行だけを作り、送料は集計側にだけ足していた。
  行の消費税には `metrics.tax`（送料税込み）を使っていた。
- **対処**: 明細に送料行（数量1）を足し、商品行の消費税は `metrics.saleTax` に変更。
  単品版の送料行も数量が商品数量（3）になっていたので 1 に直した。
- **今後**: 帳票は **「列の縦計が下段の合計と一致するか」を必ず検算**する。
  見た目のレビューだけでは通ってしまう。E2Eで行の金額を合計して小計と突き合わせるのが早い。

### 20. 複数商品の見積書だけ別の見た目・別の機能だった
- **事実**: 複数商品版の明細表だけ `font-weight:700/900` で太字だらけ。さらにツールバーの
  「明細行を追加」も行削除（×）も効かず、セルを直しても合計が再計算されなかった。
- **原因**: 複数商品版は `.itemTable` を丸ごと `.estimateItemsTable` に差し替える作りで、
  ポップアップ内のスクリプトが `.itemTable` を名指ししていたため一切当たらなかった。
  太字を禁じるテストも `simpleEstimateHtmlReference` のCSSブロックしか見ていなかった。
- **対処**: スクリプトのセレクタを `.itemTable,.estimateItemsTable` に広げ、行に `itemRow`、
  集計に `data-field` を付けて共通処理に乗せた。CSSの太字は 500 に統一。
  テストも `multiEstimateCss` を対象に追加した。
- **今後**: **同じ帳票に2系統の実装があるときは、両方に同じ検査を書く**。
  片方だけテストしていると、もう片方が別物に育っていることに気付けない。

### 21. 受注簿シートが狭い画面で「縮む」のではなく「切り取られて」いた
- **事実**: 1100px以下（小さめのノートPC・iPad・スマホ）で、受注区分〜売上日や
  商品表の右半分が画面外に切られ、入力も確認もできなかった。
- **原因**: `.orderEntrySheet { min-width: min(820px, 100%) }`。狭い画面では 100% 側が
  採用されて 366px まで縮み、中の `.entryGrid*`（`overflow:hidden`・固定カラム）が
  はみ出した分を切り落としていた。横スクロールも出ない。
- **対処**: `min-width: 820px` に固定し、狭いときは親 `.orderEntryPanel`（`overflow:auto`）を
  横スクロールさせる。あわせて `@media (max-width:1100px)` の `.entryBottom` 縦積みを削除し、
  **どの画面幅でも受注簿の中身は同じ並び**にした（画面ごとに別レイアウトにしない）。
- **今後**: A4帳票をそのまま画面に出す作りでは、**幅を縮めるのではなく横スクロールさせる**。
  `overflow:hidden` のグリッドに固定カラムを入れていると、縮めた瞬間に情報が消える。
  検証は「画面幅を変えて `scrollWidth > clientWidth` になっている要素が無いか」を機械的に見る。

### 22. 縦長の貼り付け画像が受注簿をはみ出し、PDFで切れていた
- **事実**: 縦長（900×2400など）の画像を貼ると、画像が受注簿の枠を押し広げ、
  PDFでは下半分が切り落とされていた。
- **原因**: `.sheetImage img { width:100%; height:100% }` の `height:100%` は
  親の高さが不定だと `auto` になり、画像の実寸比でそのまま伸びる。
- **対処**: 画像を `position:absolute; inset:0; margin:auto; width:auto; height:auto;
  max-width:100%; max-height:100%` にした。枠の中に収まり、比率も保たれる。
- **落とし穴**: 最初 `object-fit: contain` で収めたが、**html2canvas は object-fit を無視して
  枠いっぱいに描く**ため、PDFの中だけ写真が横に伸びていた。
  画面のスクリーンショットでは分からず、生成したPDFを見て初めて気付いた。
- **今後**: PDF化する範囲の画像は `object-fit` に頼らない。**要素の箱そのものを
  元画像の比率にする**（`max-width`/`max-height` ＋ `width:auto`/`height:auto`）。

### 23. 見積書PDFの文字サイズがウィンドウ幅で変わっていた（Learnings 2 の再発）
- **事実**: 複数商品の見積書を狭い窓でPDF保存すると、明細1行目だけ極端に小さい文字で
  焼き込まれた。広い窓では正常。
- **原因**: 明細の自動縮小 `autoFitEstimateItems()` は `load` と `beforeprint` でしか
  走らず、PDF保存時に `html.pdfExport`（A4固定）へ切り替えた**後**に測り直していなかった。
- **対処**: `documentElement` の class 変化を `MutationObserver` で見て、`pdfExport` が
  付いた時点で測り直す。1440px と 390px で生成PDFのバイト列が一致することを確認。
- **今後**: PDF化の前に **DOMを測って見た目を決める処理**があるなら、
  必ず「A4固定へ切り替えた後」に走らせる。幅違いで同じPDFが出るかの確認は毎回やる。

### 24. その他（実機操作で見つけた小さいもの）
- メール本文に別アプリの試作URL `https://ai-ui-ux-5-5-1.vercel.app/` が残っていた
  → `window.location.origin + BASE_URL` に変更。
- OCR失敗時のメッセージが `認識失敗: undefined`（worker の error は `message` を持たない）
  → 「認識に失敗しました。「再認識」を押すか、画像を貼り直してください」＋詳細は console へ。
- 印刷すると未入力欄のプレースホルダー（「備考（任意）」「自動」）がそのまま刷られていた
  → `@media print` で `::placeholder` を transparent に。
- 右下の固定ラベル「太陽シルバーサービス（株）」が受注簿の文字に重なっていた
  → `pointer-events:none` ＋ 半透明の地色で、下の操作を塞がず文字も読めるようにした。

### 検証のしかたで有効だったこと
- **数値は必ず別計算で検算する**。画面の数字をそのまま信じない。今回の3割負担の1円ズレは
  `Math.floor(c*0.7)` と `Math.floor(c*7/10)` を 0〜500,000 で全数比較して見つけた。
- **帳票は「列の縦計 ＝ 下段の合計」をE2Eで突き合わせる**。見た目のレビューでは通ってしまう。
- **PDFは必ず生成物のバイト列を取って中身を見る**。画面のスクリーンショットでは
  html2canvas 起因の崩れ（object-fit 無視・幅依存の文字サイズ）が分からない。
- 直す前に「同じ操作を複数のウィンドウ幅でやって、生成PDFのバイト列が一致するか」を
  基準にすると、幅依存の不具合を機械的に潰せる。

---

## 2026-09-15 仕様確認を1件ずつ通した回（デバッグの続き）

前回「仕様確認が必要」として残した8件を、実測値を示しながら1件ずつ判断してもらい、
決まったものから順に実装・検証・コミットした。

### 25. 給付の支給限度額は「固定値」ではなく利用者ごとの入力にする
- **事実**: 支給限度額が 200,000円 でコードに直書きされ、画面から変えられなかった。
  さらに限度額を超えた分がどこにも出ず、商品代金264,000円・1割のとき
  「利用者20,000＋保険者180,000＝200,000」で 64,000円 が消えていた。
  `careInsuranceBalance` / `livingProtection` / `fullSelfPay` / `benefitDetails` は
  `benefitCalc` が読んでいるだけで、**設定するUIが1つも無かった**。
- **判断**: 限度額を入力欄にし、超過分と利用者支払額も帳票に出す。既定値は
  特定福祉用具販売の支給限度基準額 100,000円（200,000円は住宅改修の額だった）。
- **実装**: 受注簿の先頭グリッドに「支給限度額」を追加（給付のときだけ表示）。
  給付内訳に「限度額超過分(全額自費)」「利用者支払額 合計(税込)」を追加し、
  **限度額に収まっているときは行を増やさない**（給付は「追加」だけする方針を守る）。
  印刷の先頭グリッドは列数を9→10に増やして折り返しを防いだ。
- **今後**: `benefitCalc` のように「計算はしているが画面に出ていない値」は、
  帳票の縦計が合わない原因になる。**計算結果のうち帳票に出していないものが無いか**を
  関数の返り値と表示項目を突き合わせて確認する。

### 26. 「新しいページ」の定義に漏れがあった
- **事実**: 「商品を追加」で作る新ページは商品名・定価・仕切りをクリアするのに、
  送料と数量だけ前の商品の値が残り、同じ便でも送料合計が二重になっていた。
- **対処**: `multiBlankOrder()` に `shippingFee:0, quantity:1` を追加。
  `addProduct` 側でも戻す（`le()` は再認識でも使うため、そちらの入力値は消さない）。
- **今後**: 「クリアする」処理を書いたら、**商品行に並んでいる全項目を1つずつ数えて**
  漏れがないか確認する。金額に効く項目（送料・数量）の消し忘れは金額事故になる。

### 27. 空欄を許すと、画面と帳票で別の数字になる
- **事実**: 数量を空欄にすると受注簿は「販売金額 空欄／合計 0円」なのに、
  見積書は `Math.max(1, ...)` で数量1として ¥28,000／合計 ¥30,800 を出していた。
- **対処**: 欄を離れたときに空欄なら 1 に戻す（0・負数・文字と同じ扱いに揃えた）。
  入力中は空欄のままにして、選択して打ち直す操作を妨げない。
- **今後**: 同じ値を複数の帳票で使うとき、**片方だけが「欠損値の埋め合わせ」をしていないか**
  を確認する。埋め合わせるなら入力側で確定させ、全部の帳票が同じ値を見るようにする。

### 28. state による多重起動ガードは同一フレームの連打をすり抜ける
- **事実**: PDF保存の連打で同じPDFが押した回数ぶん生成されていた。
  `useState` のフラグでガードを入れたが、**5回押して4枚生成**された。
  React の state 更新は非同期なので、同じフレームのクリックはどれも `false` を見る。
- **対処**: `useRef` に変えて同期的に判定（`pdfBusyRef.current`）。表示（ボタンの
  無効化と「PDF作成中…」）だけ state で行う。同一フレーム5回クリックで生成1つになった。
- **今後**: 「処理中は押させない」系のガードは **必ず ref（同期）で判定**する。
  state はUIの見た目にだけ使う。テストも「同一フレームで複数回 click()」で確かめる。

### 29. 不要コードの判定は、動かして確かめてから言う
- **事実**: `dedupeIntegratedTabs` の MutationObserver を「DOM変更のたびに走る
  パフォーマンス問題」と報告したが、実測すると起動時5回・商品2件の入力で追加6回だけだった。
  React の入力更新は `childList` の変更を伴わないため、ほとんど発火しない。
  **負荷の話としては誤りで、正しくは「公開ビルドでは一度も当たらない不要コード」**。
- **もう1件**: `index.html` のクリックリスナーは `textContent === "PDF"` が条件だが
  実際のボタンは「PDF保存」。一致するボタン0個で、`pdfCaptureMode` も
  `data-html2canvas-ignore` も一度も付いていなかった（対応CSSも2世代重複して死んでいた）。
- **今後**: 「重い」「無駄」と書く前に**回数を数える**。セレクタ一致を前提にした処理は
  `document.querySelectorAll(セレクタ).length` を実機で確認してから可否を言う。
  削除するときは**削除前後の生成PDFがバイト一致すること**を根拠にする。

### 30. 依存パッケージの latest 指定をやめた
- **事実**: `react` / `react-dom` / `vite` / `@vitejs/plugin-react` が `"latest"` で、
  `npm install` のたびにメジャーごと入れ替わり得る状態だった。
- **対処**: 今動いているバージョンにそのまま固定（キャレット無し）。
  `package-lock.json` は該当4行以外に差分を出さないよう手で整えた
  （npm が `name` や `libc` を書き換えるので、`git show HEAD:` の内容に4箇所だけ当てた）。
  `npm ci` で入れ直してビルド成果物がバイト一致することを確認。
- **今後**: 業務ツールの依存は固定する。ロックファイルに意図しない差分が出たら、
  **元のファイルに必要な置換だけを当て直す**のが一番確実。

### 既存テストが「実装の文字列」を固定していて直しの邪魔になった
- 今回 `benefit-layout.test.mjs` の `.64fr .72fr .66fr`、`order-pdf.test.mjs` の
  `children:`PDF保存`}` と `finally{printCss.remove();}` が、正しい修正を入れたときに
  落ちた。いずれも**意図ではなく実装の見た目**を固定していたため。
- 直すときは「列数が項目数と一致すること」「印刷CSSを必ず外すこと」のように
  **意図を書き直す**。新しく足すテストも、リテラル一致より意図の検査を優先する。

### 31. 支給限度額は「給付種別ごと」の基準額にした（25の続き）
- **事実**: 既定値を特定福祉用具の10万円1本にしたが、実際は
  **住宅改修 200,000円／特定福祉用具 100,000円**と、給付種別で基準額が違う。
- **対処**: 受注簿に「給付種別」（特定福祉用具／住宅改修）の欄を追加し、
  `BENEFIT_LIMIT_BY_TYPE` から既定額を引く。種別を選び直すと支給限度額も
  その種別の基準額に入れ替え、手入力での上書きもできる。
  空欄のまま欄を離れたら種別の基準額に戻す（**表示している額＝計算に使った額**を守る）。
- **落とし穴**: 追加した `<select>` に `className:no-print` を付け忘れ、印刷・PDFで
  「住宅改修」が**2回**出た（select の値と `.printValue` の両方）。
  この受注簿では、セレクトは `no-print` ＋ `.printValue` が既存の作法
  （SMARTれん太の販売分類が同じ形）。radio/checkbox は印刷CSSが隠すので不要。
- **今後**: 先頭グリッドに項目を足したら、**印刷用の列数指定
  （`.benefitMode .entryGridPrimary`）も同じ数に増やす**。忘れると売上日だけが
  次の行に落ちる。テストは列幅の数値ではなく「列数＝項目数」で検査する。
